import {StyleSheet, Text, View} from 'react-native';
import React from 'react';
import NewFeedView from './newFeed.view';

const NewFeedContainer = () => {
  return <NewFeedView />;
};

export default NewFeedContainer;
const styles = StyleSheet.create({});
